<h1 class="text-left">Create a Profile</h1>

<div class="row">
  <div class="span6">
	<%= form_for(@organization) do |f| %>
	  <%= render 'shared/error_messages', :object => f.object %>
	  <%= render 'fields', :f => f %>
	  <div class="actions btn">
	    <%= f.submit "Submit", class: 'btn offset4' %>
	  </div>
	<% end %>
 </div>
</div>
